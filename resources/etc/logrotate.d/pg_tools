
/var/log/pg_tools/pg_tools.log {
    daily
    rotate 10
    missingok
    notifempty
    compress
    sharedscripts
    postrotate
        [ -f /var/run/pg_tools/pg_tools.pid ] && kill -USR1 `cat /var/run/pg_tools/pg_tools.pid`
    endscript
}